{
  "name": "keeping-cadence-api",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Serverless API for Keeping Cadence (Neon Postgres backend).",
  "engines": { "node": ">=18" },
  "dependencies": {
    "@neondatabase/serverless": "^0.9.5",
    "jsonwebtoken": "^9.0.2",
    "stripe": "^16.12.0"
  }
}
